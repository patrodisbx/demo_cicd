name: CI

on:
  pull_request:
    branch: [development]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: check out repo
        uses: actions/checkout@v2

      - name: list all files
        run: dir

      - name: login to vctl
        run: vctl\vctl.exe login https://vsystem.ingress.dh-ifc1k6ng.di-us-east.shoot.live.k8s-hana.ondemand.com disbxt01 ${{secrets.USER_NAME}} -p ${{secrets.PASSWORD}}
   
      - name: bundle solution
        run: vctl solution bundle Kafka_CICD 

      - name: upload solution
        run: vctl solution upload Kafka_CICD.zip
        
